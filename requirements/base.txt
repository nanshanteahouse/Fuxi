# ═══════════════════════════════════════════════════════════════════════
# Fuxi — 共享核心依赖 (薄包装)
# ═══════════════════════════════════════════════════════════════════════
#
# 声明源: pyproject.toml [project] dependencies
#   17 包: numpy / scipy / pandas / scanpy / anndata / pydantic / openai / hdf5plugin /
#          matplotlib / scikit-learn / networkx / psutil / joblib / pyyaml /
#          openpyxl / python-dotenv / textual
#
# 等价: pip install -e "."    (仅 base,不含模态/方法学 extras)
# ═══════════════════════════════════════════════════════════════════════
-e .
