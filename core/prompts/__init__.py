"""Prompts package — all LLM prompt templates for Fuxi.

Each YAML file contains:
  system: |         # system prompt (multi-line block)
    ...
  user_template: |  # user prompt template with {placeholders}
    ...
"""
