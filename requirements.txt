# ═══════════════════════════════════════════════════════════════════════
# Fuxi (伏羲) — 统一单细胞多组学管线依赖
# ═══════════════════════════════════════════════════════════════════════
#
# 安装全部模态: pip install -r requirements.txt
# 仅安装 RNA:   pip install -r requirements/rna.txt
# 仅安装 ATAC:   pip install -r requirements/atac.txt
# 仅安装空间:   pip install -r requirements/spatial.txt
#
# 文件结构:
#   requirements/
#   ├── base.txt     共享核心 (numpy, scipy, pandas, scanpy, pydantic ...)
#   ├── rna.txt       scRNA-seq  (harmonypy, Scrublet, decoupler, LIANA ...)
#   ├── atac.txt      scATAC-seq (Snapatac2, Mudata, polars ...)
#   ├── spatial.txt   spatial    (Squidpy, LIANA ...)
#   ├── paper.txt     论文工具   (pymupdf4llm, pymupdf)
#   ├── dev.txt       开发测试   (pytest, ruff, deepdiff) — 不进 all.txt
#   └── all.txt       全部模态 + 论文工具
# ═══════════════════════════════════════════════════════════════════════

-r requirements/all.txt
