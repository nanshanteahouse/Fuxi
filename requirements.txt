# ═══════════════════════════════════════════════════════════════════════
# Fuxi (伏羲) — 统一单细胞多组学管线
# ═══════════════════════════════════════════════════════════════════════
#
# 依赖单一声明源: pyproject.toml
#   [project] dependencies              → base 运行时 (16 包)
#   [project.optional-dependencies]     → 模态 / 方法学 / 聚合 extras
#
# 本文件是便利别名,等价于:  pip install -e ".[all]"
#
# 常用安装方式:
#   全量模态:    pip install -r requirements.txt            (本文件)
#   单模态:      pip install -r requirements/rna.txt        (等价 -e ".[rna]")
#   +方法学:     pip install -e ".[rna,celltypist]"
#   轻量方法包:  pip install -e ".[methods]"                (scvi+celltypist+scvelo+sccoda)
#
# ⚠️ [all] 不含方法学(scvi/celltypist/scvelo/sccoda)——避免拉入 PyTorch。
#    需要时显式加 [methods] 或单个 extra。
#
# 详见 pyproject.toml [project.optional-dependencies] 获取全部 extras 列表。
# ═══════════════════════════════════════════════════════════════════════
-e ".[all]"
